<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorRoyalBlue</item>
        <item name="colorPrimaryDark">@color/colorRoyalBlue</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>-->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">true</item>
        <item name="android:fontFamily">@font/ubuntu_regular</item>
        <item name="fontFamily">@font/ubuntu_regular</item>
    </style>
    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/colorRoyalBlue</item>
        <item name="colorPrimaryDark">@color/colorRoyalBlue</item>
        <item name="android:textColorPrimary">@color/enlightenmentPink</item>
        <item name="android:textColorSecondary">@color/enlightenmentPinkHint</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>

</resources>
